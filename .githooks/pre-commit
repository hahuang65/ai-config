#!/usr/bin/env bash
set -euo pipefail

REPO_DIR="$(git rev-parse --show-toplevel)"

# Validate pipeline integrity
if [[ -f "$REPO_DIR/scripts/test-pipeline.sh" ]]; then
  bash "$REPO_DIR/scripts/test-pipeline.sh"
fi
