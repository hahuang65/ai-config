---
description: Create a detailed implementation plan for a feature or change, then refine it through annotation cycles
---
Load the plan skill, then create a detailed implementation plan for: $ARGUMENTS

Follow the plan skill workflow. Check for existing research documents in `docs/claude/`, read relevant source files, and base the plan on the actual codebase. Write a persistent plan document in `docs/claude/` with goal, approach, detailed changes with code snippets, dependencies, trade-offs, and testing strategy.

Stop after writing the plan and wait for the user to annotate and review before proceeding.
