---
description: Synchronize trunk and rebase the active Orchard task onto it
argument-hint: "[intent]"
---

Load and follow the `orchard` skill for its rebase operation, forwarding `$ARGUMENTS` unchanged.
