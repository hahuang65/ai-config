---
description: Resolve a rebase, merge, or workflow-owned working-state conflict with human decisions for incompatible hunks
argument-hint: "[operation-goal-or-intent]"
---

Load and follow the `resolve-conflicts` skill, forwarding `$ARGUMENTS` unchanged.
