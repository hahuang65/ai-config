---
description: Full feature workflow — research deeply, plan with annotation cycles, then implement everything
---
Load the build-feature skill, then build: $ARGUMENTS

Follow the build-feature skill workflow through all three phases:
1. **Research** — deep-read the relevant codebase area, produce a research document, generate an architecture diagram
2. **Plan** — create a detailed implementation plan, iterate through annotation cycles until approved, generate a visual plan
3. **Implement** — execute all tasks from the approved plan, then generate a fact-checked diff review

Stop at each phase gate and wait for the user to confirm before proceeding.
